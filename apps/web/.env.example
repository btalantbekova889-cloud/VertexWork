# URL backend API, доступный с сервера, на котором запущен Next.js (server-side proxy).
# В dev — порт локального backend; в production — обычно тот же сервер, localhost:4000.
API_INTERNAL_URL=http://localhost:4000
